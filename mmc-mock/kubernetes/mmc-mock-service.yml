apiVersion: v1
kind: Service
metadata:
  name: mmc-mock
  labels:
    name: mmc-mock
spec:
  type: LoadBalancer
  ports:
   - name: http
     port: 80
     targetPort: 8080
  selector:
    app: mmc-mock
